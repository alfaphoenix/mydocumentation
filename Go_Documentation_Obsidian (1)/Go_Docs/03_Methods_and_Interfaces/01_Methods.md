---
tags: [golang, methods, receiver]
aliases: [Go Методы, Go Ресиверы]
---

# 🛠️ Методы

Методы — это функции, привязанные к определенному типу (ресиверу).

## 1. Объявление Метода

Метод объявляется с помощью специального аргумента — **ресивера** — между ключевым словом `func` и именем метода.

```go
type Vertex struct {
	X, Y float64
}

// Метод Abs привязан к типу Vertex
func (v Vertex) Abs() float64 {
	return math.Sqrt(v.X*v.X + v.Y*v.Y)
}

func main() {
	v := Vertex{3, 4}
	fmt.Println(v.Abs()) // Вызов метода
}
```

## 2. Ресивер-Значение vs Ресивер-Указатель

Выбор типа ресивера (`T` или `*T`) определяет, может ли метод изменять значение, на котором он вызван.

| Тип Ресивера | Синтаксис | Может Изменять Значение? | Когда Использовать |
| :--- | :--- | :--- | :--- |
| **Значение** | `(v T)` | Нет (работает с копией). | Для методов, которые только читают данные. |
| **Указатель** | `(v *T)` | Да (работает с оригиналом). | Для методов, которые должны изменять данные ресивера или для больших структур. |

### Пример Ресивера-Указателя

```go
// Метод Scale привязан к указателю на Vertex
func (v *Vertex) Scale(f float64) {
	v.X = v.X * f
	v.Y = v.Y * f
}

func main() {
	v := Vertex{3, 4}
	v.Scale(10) // Go автоматически преобразует v в &v
	fmt.Println(v) // {30 40} - значение v изменено
}
```

## 3. Методы и Не-Структурные Типы

Методы могут быть объявлены для любого типа, кроме указателей и интерфейсов, при условии, что тип определен в том же пакете, что и метод.

```go
type MyFloat float64

func (f MyFloat) Abs() float64 {
	if f < 0 {
		return float64(-f)
	}
	return float64(f)
}

func main() {
	f := MyFloat(-4.0)
	fmt.Println(f.Abs()) // 4
}
```
