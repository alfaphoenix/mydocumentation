---
tags: [golang, pointers, memory]
aliases: [Go Указатели]
---

# 📍 Указатели (Pointers)

Указатели хранят адрес переменной в памяти.

## 1. Объявление и Операторы

| Оператор | Название | Описание |
| :--- | :--- | :--- |
| `&` | **Адрес** | Возвращает адрес переменной (указатель). |
| `*` | **Разыменование** | Возвращает значение, на которое указывает указатель. |

```go
i := 42 // i - переменная типа int
p := &i // p - указатель на i (*int)

fmt.Println(*p) // Чтение i через указатель: 42

*p = 21 // Установка i через указатель
fmt.Println(i) // i теперь 21
```

## 2. Нулевое Значение Указателя

Нулевое значение для указателя — `nil`.

```go
var p *int // p == nil
```

## 3. Указатели и Функции

Передача указателя в функцию позволяет функции изменять значение исходной переменной.

```go
func zeroval(ival int) {
	ival = 0 // Изменяет только копию
}

func zeroptr(iptr *int) {
	*iptr = 0 // Изменяет значение по адресу
}

func main() {
	i := 1
	zeroval(i)
	fmt.Println("zeroval:", i) // 1

	zeroptr(&i) // Передаем адрес i
	fmt.Println("zeroptr:", i) // 0
}
```

## 4. Указатели и Структуры

При доступе к полю структуры через указатель Go автоматически разыменовывает указатель.

```go
type Vertex struct {
	X, Y int
}

v := Vertex{1, 2}
p := &v
p.X = 10 // Эквивалентно (*p).X = 10
fmt.Println(v.X) // 10
```
