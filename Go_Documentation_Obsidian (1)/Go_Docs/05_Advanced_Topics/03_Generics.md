---
tags: [golang, generics, advanced]
aliases: [Go Дженерики, Go Обобщенное Программирование]
---

# 🧬 Дженерики (Generics)

Дженерики (обобщенное программирование) были добавлены в Go 1.18 и позволяют писать функции и типы, которые работают с любым из набора типов.

## 1. Параметры Типа

Параметры типа объявляются в квадратных скобках `[]`.

```go
// Функция, которая может работать с int64 или float64
func SumIntsOrFloats[K comparable, V int64 | float64](m map[K]V) V {
	var s V
	for _, v := range m {
		s += v
	}
	return s
}
```

## 2. Ограничения (Constraints)

Ограничения определяют набор типов, которые могут быть использованы в качестве аргументов типа.

### Предопределенные Ограничения

*   `any`: Эквивалент `interface{}`, любой тип.
*   `comparable`: Типы, которые можно сравнивать с помощью `==` или `!=` (например, все базовые типы, кроме срезов, карт и функций).

### Пользовательские Ограничения

Ограничения объявляются как интерфейсы, которые могут включать:

1.  Набор методов (как обычные интерфейсы).
2.  Набор типов (используя оператор `|`).

```go
// Пользовательское ограничение Number
type Number interface {
	int64 | float64
}

// Использование пользовательского ограничения
func SumNumbers[K comparable, V Number](m map[K]V) V {
	var s V
	for _, v := range m {
		s += v
	}
	return s
}
```

## 3. Обобщенные Типы

Дженерики также могут использоваться для определения обобщенных типов.

```go
// Обобщенная структура List
type List[T any] struct {
	head, tail *element[T]
}

type element[T any] struct {
	next *element[T]
	val  T
}

// Обобщенный метод
func (lst *List[T]) Push(v T) {
	// ...
}
```
