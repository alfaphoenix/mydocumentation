---
tags: [golang, standard-library, packages]
aliases: [Go Стандартная Библиотека]
---

# 📚 Обзор Стандартной Библиотеки

Стандартная библиотека Go (Go Standard Library) обширна и является одной из сильных сторон языка.

## 1. Основные Пакеты

| Пакет | Описание |
| :--- | :--- |
| **`fmt`** | Форматированный ввод/вывод (печать, сканирование). |
| **`os`** | Функции операционной системы (файлы, переменные окружения, аргументы командной строки). |
| **`io`** | Примитивы ввода/вывода (`Reader`, `Writer`). |
| **`strings`** | Функции для работы со строками. |
| **`strconv`** | Преобразование строк в базовые типы и обратно. |
| **`time`** | Функции для работы со временем и датами. |
| **`log`** | Простой логгер. |
| **`testing`** | Поддержка автоматического тестирования. |

## 2. Сеть и HTTP

| Пакет | Описание |
| :--- | :--- |
| **`net/http`** | HTTP-клиент и сервер. Включает в себя маршрутизацию, обработку запросов и ответов. |
| **`net`** | Низкоуровневые сетевые операции (TCP, UDP, IP). |
| **`encoding/json`** | Кодирование и декодирование JSON. |
| **`encoding/xml`** | Кодирование и декодирование XML. |

## 3. Параллелизм и Синхронизация

| Пакет | Описание |
| :--- | :--- |
| **`sync`** | Примитивы синхронизации (`Mutex`, `RWMutex`, `WaitGroup`). |
| **`sync/atomic`** | Низкоуровневые атомарные операции для примитивов. |
| **`context`** | Управление контекстом, таймаутами и отменой в горутинах. |

## 4. Файловая Система

| Пакет | Описание |
| :--- | :--- |
| **`os`** | Работа с файлами и каталогами (`os.Open`, `os.Create`). |
| **`path/filepath`** | Кроссплатформенная работа с путями к файлам. |
| **`bufio`** | Буферизованный ввод/вывод (для эффективного чтения/записи). |

## 5. Примеры Использования

### HTTP-Сервер

```go
package main

import (
	"fmt"
	"net/http"
)

func handler(w http.ResponseWriter, r *http.Request) {
	fmt.Fprintf(w, "Hello, %s!", r.URL.Path[1:])
}

func main() {
	http.HandleFunc("/", handler) // Установка обработчика
	http.ListenAndServe(":8080", nil) // Запуск сервера
}
```

### JSON Кодирование/Декодирование

```go
type Response struct {
	Name string `json:"name"`
	Age  int    `json:"age"`
}

func main() {
	// Кодирование (Marshal)
	res := Response{Name: "Gopher", Age: 10}
	b, _ := json.Marshal(res)
	fmt.Println(string(b)) // {"name":"Gopher","age":10}

	// Декодирование (Unmarshal)
	var res2 Response
	json.Unmarshal(b, &res2)
	fmt.Println(res2.Name) // Gopher
}
```
