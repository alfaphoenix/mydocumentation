---
tags: [golang, basics, variables, types, constants]
aliases: [Go Переменные, Go Типы Данных]
---

# 📝 Переменные, Типы и Константы

## 1. Объявление Переменных

Go — это **статически типизированный** язык. Тип переменной должен быть известен во время компиляции.

### Полное Объявление

```go
var name type = expression
```

**Пример:**

```go
var i int = 10
var f float64 = 3.14
var s string = "Hello"
```

### Вывод Типа

Если инициализатор присутствует, тип может быть опущен. Компилятор выведет тип автоматически.

```go
var i = 10         // int
var f = 3.14       // float64
var b = true       // bool
```

### Короткое Объявление (`:=`)

Используется для объявления и инициализации переменной внутри функции.

```go
i := 10         // int
s := "World"    // string
```

> **Важно:** Оператор `:=` можно использовать только для объявления **новых** переменных. Для присвоения значения существующей переменной используйте `=`.

## 2. Основные Типы Данных

| Категория | Типы | Описание |
| :--- | :--- | :--- |
| **Булевы** | `bool` | `true` или `false`. |
| **Числовые** | `int`, `int8`, `int16`, `int32`, `int64` | Целые числа со знаком. |
| | `uint`, `uint8`, `uint16`, `uint32`, `uint64`, `uintptr` | Целые числа без знака. |
| | `float32`, `float64` | Числа с плавающей точкой. |
| | `complex64`, `complex128` | Комплексные числа. |
| **Строки** | `string` | Последовательность байтов (UTF-8). Неизменяемы. |
| **Прочие** | `byte` (псевдоним для `uint8`), `rune` (псевдоним для `int32`, представляет кодовую точку Unicode). |

## 3. Нулевые Значения (Zero Values)

Переменные, объявленные без явного инициализатора, автоматически инициализируются **нулевым значением** своего типа.

| Тип | Нулевое Значение |
| :--- | :--- |
| **Числовые** | `0` |
| **Булевы** | `false` |
| **Строки** | `""` (пустая строка) |
| **Указатели, Срезы, Карты, Каналы** | `nil` |

## 4. Константы

Константы объявляются с помощью ключевого слова `const`. Они могут быть только символьными, строковыми, булевыми или числовыми.

```go
const Pi = 3.14159
const World = "мир"
const Truth = true
```

### Нетипизированные Числовые Константы

Числовые константы в Go имеют высокую точность и не имеют типа до тех пор, пока не будут использованы в контексте, требующем типа.

```go
const Big = 1 << 100 // Очень большое число
const Small = Big >> 99 // 2

fmt.Println(Small) // 2 (тип int)
fmt.Println(Small * 0.5) // 1.0 (тип float64)
```

### `iota`

`iota` — это предопределенный идентификатор, который используется для объявления последовательности нетипизированных целочисленных констант. Значение `iota` равно 0 в первом объявлении `const` и увеличивается на 1 в каждой последующей строке.

```go
const (
	A = iota // 0
	B = iota // 1
	C = iota // 2
)

const (
	_ = iota // Пропускаем 0
	KB = 1 << (10 * iota) // 1 << 10 = 1024
	MB = 1 << (10 * iota) // 1 << 20
	GB = 1 << (10 * iota) // 1 << 30
)
```
