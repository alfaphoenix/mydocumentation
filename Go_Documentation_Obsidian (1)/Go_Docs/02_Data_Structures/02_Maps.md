---
tags: [golang, data-structures, map]
aliases: [Go Карты, Go Хеш-таблицы]
---

# 🗺️ Карты (Maps)

Карты (или хеш-таблицы) — это неупорядоченные коллекции пар ключ-значение.

## 1. Создание Карты

### С помощью `make`

```go
// Создание пустой карты, где ключи - string, значения - int
m := make(map[string]int)
```

### С помощью Литерала

```go
m := map[string]int{
	"apple":  1,
	"banana": 2,
}
```

## 2. Операции с Картами

| Операция | Синтаксис | Описание |
| :--- | :--- | :--- |
| **Вставка/Обновление** | `m[key] = value` | Добавляет новую пару или обновляет существующую. |
| **Получение** | `value := m[key]` | Получает значение по ключу. |
| **Удаление** | `delete(m, key)` | Удаляет пару ключ-значение. |
| **Проверка наличия** | `value, ok := m[key]` | Идиоматический способ проверить, существует ли ключ. |

### Проверка Наличия Ключа

Go предоставляет идиоматический способ проверить, существует ли ключ в карте, используя синтаксис с двумя возвращаемыми значениями.

```go
m := make(map[string]int)
m["Answer"] = 42

// Получение значения и проверка наличия
v, ok := m["Answer"]
fmt.Println("Значение:", v, "Присутствует:", ok) // Значение: 42 Присутствует: true

v, ok = m["Question"]
fmt.Println("Значение:", v, "Присутствует:", ok) // Значение: 0 Присутствует: false
```

> **Важно:** Если ключ отсутствует, Go вернет **нулевое значение** типа значения карты (в примере выше `0` для `int`). Поэтому всегда используйте `ok` для проверки наличия.

## 3. Итерация по Картам

Используйте цикл `for range` для итерации по ключам и значениям карты. Порядок итерации **не гарантируется**.

```go
kvs := map[string]string{"a": "apple", "b": "banana", "c": "cherry"}
for k, v := range kvs {
	fmt.Printf("%s -> %s\n", k, v)
}
```

## 4. Карты и `nil`

Нулевая карта (`nil`) имеет длину 0. Попытка добавить элемент в нулевую карту вызовет панику.

```go
var m map[string]int // m == nil

// m["key"] = 1 // Паника!

// Для безопасного использования нужно инициализировать:
m = make(map[string]int)
m["key"] = 1 // OK
```
