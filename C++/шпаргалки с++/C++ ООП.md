
## 🧱 1. Инкапсуляция (encapsulation)

**Смысл:**  
Спрятать внутреннюю реализацию и дать наружу только безопасный интерфейс.

👉 _Пользователь объекта не должен знать, **как** он устроен, только **что** он умеет._

```cpp
class BankAccount {
private:
    int balance;   // скрыто

public:
    BankAccount(int b) {
        balance = b;
    }

    void deposit(int amount) {
        balance += amount;
    }

    int getBalance() {
        return balance;
    }
};
```

```cpp
int main() {
    BankAccount acc(100);
    acc.deposit(50);
    // acc.balance = 100000; ❌ нельзя
    cout << acc.getBalance(); // ✅
}
```

💡 **Зачем?**  
Чтобы:

- защитить данные
    
- контролировать изменения
    
- меньше ломать код при изменениях
    

---

## 🧬 2. Наследование (inheritance)

**Смысл:**  
Один класс может **наследовать** другой и расширять его.

```cpp
class Animal {
public:
    void eat() {
        cout << "Животное ест\n";
    }
};

class Dog : public Animal {
public:
    void bark() {
        cout << "Собака лает\n";
    }
};
```

```cpp
int main() {
    Dog dog;
    dog.eat();   // от Animal
    dog.bark();  // свой
}
```

💡 **Зачем?**

- переиспользование кода
    
- логическая иерархия
    
- «Dog — это Animal»
    

---

## 🎭 3. Полиморфизм (polymorphism)

**Смысл:**  
Одинаковый интерфейс — разное поведение.

⚠️ **Ключ:** `virtual`

```cpp
class Animal {
public:
    virtual void makeSound() {
        cout << "Какой-то звук\n";
    }
};

class Dog : public Animal {
public:
    void makeSound() override {
        cout << "Гав!\n";
    }
};

class Cat : public Animal {
public:
    void makeSound() override {
        cout << "Мяу!\n";
    }
};
```

```cpp
int main() {
    Animal* a1 = new Dog();
    Animal* a2 = new Cat();

    a1->makeSound(); // Гав!
    a2->makeSound(); // Мяу!
}
```

💡 **Зачем?**

- писать код, который работает с **базовым классом**
    
- легко добавлять новые типы без переписывания логики
    

---

## 🧠 4. Абстракция (abstraction)

**Смысл:**  
Описать **что делать**, но не **как**.

Используются **абстрактные классы** (чисто виртуальные функции):

```cpp
class Shape {
public:
    virtual double area() = 0; // чисто виртуальная функция
};
```

```cpp
class Rectangle : public Shape {
    double w, h;
public:
    Rectangle(double w, double h) : w(w), h(h) {}

    double area() override {
        return w * h;
    }
};
```

```cpp
int main() {
    Shape* s = new Rectangle(3, 4);
    cout << s->area();
}
```

💡 **Зачем?**

- задать контракт
    
- заставить наследников реализовать нужное поведение
    

---

## 📌 Краткая шпаргалка

|Принцип|Что даёт|
|---|---|
|Инкапсуляция|безопасность и контроль|
|Наследование|переиспользование|
|Полиморфизм|гибкость|
|Абстракция|масштабируемость|

---

## 🧠 Как это выглядит в реальном коде?

Пример из жизни:

- `Shape` — интерфейс
    
- `Circle`, `Rectangle` — реализации
    
- код работает с `Shape*`, а не с конкретным типом
    

Так пишут **движки, UI, Qt, Unreal, backend, всё**.
