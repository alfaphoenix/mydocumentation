Конкатенация строк — это просто **склеивание строк** в одну. В C++ есть несколько способов, и тут важно **чем именно ты пользуешься**.

---

## 1️⃣ `std::string` (нормальный и правильный способ)

```cpp
#include <string>

std::string a = "Hello";
std::string b = " world";

std::string c = a + b;        // "Hello world"
a += "!";                     // "Hello!"
```

✔ безопасно  
✔ удобно  
✔ читаемо

💡 Работает **только если хотя бы одна сторона — `std::string`**

```cpp
std::string s = "Hello " + std::string("world"); // ок
// "Hello " + "world"; ❌ не работает
```

---

## 2️⃣ Конкатенация + числа

```cpp
int x = 42;

std::string s = "Answer: " + std::to_string(x);
// "Answer: 42"
```

Или через поток:

```cpp
#include <sstream>

std::ostringstream oss;
oss << "Answer: " << x;
std::string s = oss.str();
```

---

## 3️⃣ Через `append()`

```cpp
std::string s = "Hello";
s.append(" ");
s.append("world");
```

То же самое, просто без `+`.

---

## 4️⃣ C-style строки (`char*`, `char[]`) ⚠️ аккуратно

Тут **нет оператора `+`**.

```cpp
char a[20] = "Hello";
char b[] = " world";

strcat(a, b);   // a = "Hello world"
```

⚠️ Опасно:

- нет проверки размера
    
- легко получить UB
    

Лучше так **не делать**, если можно `std::string`.

---

## 5️⃣ `std::format` (C++20 🔥)

```cpp
#include <format>

std::string s = std::format("{} {}", "Hello", "world");
```

Очень удобно для сложных строк.

---

## 6️⃣ Когда что использовать

|Ситуация|Что брать|
|---|---|
|Обычный код|`std::string +`|
|Много склеиваний|`reserve()` + `+=`|
|Логи / формат|`std::format`|
|Старый C-код|`strcat` (осторожно)|

---
