---
tags: go golang cheatsheet programming
aliases: [Go Шпаргалка, Golang Cheat Sheet]
---

# 🚀 Большая шпаргалка по Go (Golang)

Эта шпаргалка представляет собой краткий справочник по основным концепциям, синтаксису, стандартной библиотеке и лучшим практикам языка программирования Go, оптимизированный для использования в **Obsidian**.

---

## 1. 🧱 Основы и Синтаксис

### Структура Программы

Каждая программа на Go состоит из пакетов. Выполнение начинается с функции `main` в пакете `main`.

| Элемент | Описание |
| :--- | :--- |
| **Пакет** | Объявление пакета. Исполняемые программы используют `package main`. |
| **Импорт** | Импорт других пакетов. Используйте скобки для группировки. |
| **Функция `main`** | Точка входа в исполняемую программу. |

```go
package main

import (
	"fmt"
	"math"
)

func main() {
	fmt.Println("Привет, Go!")
	fmt.Println("Квадратный корень из 4:", math.Sqrt(4))
}
```

### Переменные и Константы #переменные #константы

| Объявление | Описание | Пример |
| :--- | :--- | :--- |
| **Полное** | Объявление с типом и начальным значением. | `var i int = 10` |
| **Короткое** | Вывод типа (`:=`). **Только внутри функций.** | `j := 20` |
| **Константы** | Объявляются с помощью `const`. Нельзя использовать `:=`. | `const Pi = 3.14` |

> [!WARNING] Короткое объявление
> Оператор `:=` (короткое объявление) можно использовать **только внутри функций**. Вне функций используйте `var` или `const`.

> [!NOTE] Нулевые значения
> Если переменная объявлена без начального значения, ей присваивается **нулевое значение** типа: `0` для чисел, `false` для булевых, `""` для строк, `nil` для указателей, срезов, карт, каналов, функций и интерфейсов.

### Основные Типы Данных #типы

| Категория | Типы | Описание |
| :--- | :--- | :--- |
| **Числа** | `int`, `int64`, `float64`, `complex128` | Целые, числа с плавающей точкой. |
| **Логический** | `bool` | `true` или `false`. |
| **Строки** | `string` | Неизменяемая последовательность байтов. |
| **Байты/Руны** | `byte` (uint8), `rune` (int32, символ Unicode). |

---

## 2. ⚙️ Управление Потоком #управление_потоком

### Условные Операторы (`if`, `else`)

```go
func conditional(x int) {
	// `if` с необязательным коротким оператором (переменная v видна только внутри if/else)
	if v := x * 2; v > 10 {
		fmt.Println("v > 10:", v)
	} else if v > 5 {
		fmt.Println("5 < v <= 10:", v)
	} else {
		fmt.Println("v <= 5:", v)
	}
}
```

### Циклы (`for`)

В Go есть только один цикл — `for`.

| Тип Цикла | Описание | Пример |
| :--- | :--- | :--- |
| **Стандартный** | Инициализация; Условие; Пост-оператор. | `for i := 0; i < 5; i++ { ... }` |
| **`while`** | Только Условие. | `for sum < 100 { sum += sum }` |
| **Бесконечный** | Без Условия. | `for { ... }` |
| **`range`** | Итерация по коллекциям. | `for i, v := range slice { ... }` |

> [!TIP] Пропуск значений в `range`
> Если вам не нужен индекс или значение, используйте символ подчеркивания `_`:
> *   `for _, value := range slice { ... }` (пропуск индекса)
> *   `for index, _ := range slice { ... }` (пропуск значения)

### Переключатель (`switch`)

`switch` в Go не требует `break` (он неявно присутствует).

```go
func switchExample(day int) {
	switch day {
	case 1, 7: // Несколько значений в одном case
		fmt.Println("Выходной")
	default:
		fmt.Println("Рабочий день")
	}

	// switch без выражения (аналог if/else if/else)
	t := 10
	switch {
	case t < 5:
		fmt.Println("Мало")
	case t >= 5:
		fmt.Println("В самый раз")
	}
}
```

---

## 3. 🧩 Структуры Данных #коллекции

### Массивы и Срезы (Arrays and Slices) #срез #массив

| Тип | Описание |
| :--- | :--- |
| **Массив** | Фиксированный размер. Редко используется напрямую. |
| **Срез** | Динамический размер. **Основной тип для коллекций.** |

```go
// Создание среза
s := make([]int, 5, 10) // len=5, cap=10

// Добавление элементов
s = append(s, 4, 5)

// Слайсинг: [начало:конец] (конец не включается)
t := []int{1, 2, 3, 4, 5}
sub := t[1:4] // [2 3 4]
```

> [!CAUTION] Срезы — это ссылки
> Срез — это дескриптор, который ссылается на базовый массив. Изменение элемента в срезе **изменит** элемент в базовом массиве и во всех других срезах, ссылающихся на этот же массив.

### Карты (Maps) #карта

Карты — это неупорядоченные коллекции пар ключ-значение.

| Операция | Описание | Пример |
| :--- | :--- | :--- |
| **Создание** | Использование `make` или литерала. | `m := make(map[string]int)` |
| **Проверка наличия** | Возвращает значение и `bool` флаг. | `v, ok := m["key"]` |
| **Удаление** | | `delete(m, "key")` |

```go
m := map[string]int{"apple": 1}

// Проверка наличия
v, ok := m["apple"]
if ok {
	fmt.Println("Значение apple:", v)
}
```

---

## 4. 🛠️ Функции, Методы и Интерфейсы

### Функции #функции

Функции могут возвращать несколько значений.

```go
// Функция с несколькими возвращаемыми значениями
func swap(x, y string) (string, string) {
	return y, x
}

// Функция с именованными возвращаемыми значениями (naked return)
func split(sum int) (x, y int) {
	x = sum * 4 / 9
	y = sum - x
	return // Возвращает x и y
}
```

### Методы #методы

Метод — это функция с *получателем* (receiver).

| Тип Получателя | Описание | Когда использовать |
| :--- | :--- | :--- |
| **Значение** (`v Vertex`) | Работает с копией структуры. | Для чтения данных. |
| **Указатель** (`v *Vertex`) | Работает с оригинальной структурой. | Для изменения данных. |

```go
type Vertex struct {
	X, Y float64
}

// Метод с получателем-указателем (изменяет v)
func (v *Vertex) Scale(f float64) {
	v.X = v.X * f
	v.Y = v.Y * f
}
```

### Интерфейсы #интерфейсы

Интерфейс — это набор сигнатур методов. Тип реализует интерфейс **неявно**, если он реализует все его методы.

```go
type Abser interface {
	Abs() float64
}

// MyFloat неявно реализует Abser
type MyFloat float64
func (f MyFloat) Abs() float64 {
	// ... реализация
}
```

> [!NOTE] Пустой интерфейс и `any`
> *   **Пустой интерфейс** (`interface{}`) может содержать значение любого типа.
> *   Начиная с Go 1.18, рекомендуется использовать псевдоним **`any`** вместо `interface{}`.

---

## 5. ⚡ Параллелизм (Concurrency) #concurrency #goroutine #channel

Go использует **Goroutines** (легковесные потоки) и **Channels** (каналы для обмена данными).

### Goroutines

Запускаются с помощью ключевого слова `go`.

```go
func goroutineExample() {
	go say("мир") // Запуск в новой горутине
	say("привет") // Запуск в основной горутине
	// Необходимо использовать sync.WaitGroup или канал, чтобы дождаться завершения
}
```

### Channels

Каналы используются для безопасной передачи данных между горутинами.

| Операция | Синтаксис |
| :--- | :--- |
| **Создание** | `ch := make(chan int)` |
| **Отправка** | `ch <- 10` |
| **Прием** | `x := <-ch` |
| **Буферизованный** | `ch := make(chan int, 100)` |

### `select`

Позволяет горутине ждать нескольких операций канала.

```go
select {
case msg1 := <-c1:
	fmt.Println("Получено из c1:", msg1)
case msg2 := <-c2:
	fmt.Println("Получено из c2:", msg2)
default:
	// Выполняется, если ни один канал не готов
}
```

---

## 6. ✨ Дженерики (Generics) - Go 1.18+ #generics

Дженерики позволяют писать функции и типы, которые работают с любым из набора типов.

### Типовые Параметры и Ограничения

Типовые параметры объявляются в квадратных скобках `[]`.

| Ограничение | Описание |
| :--- | :--- |
| **`any`** | Любой тип. |
| **`comparable`** | Типы, которые можно сравнивать (`==` и `!=`). |
| **Объединение** | Список типов, разделенных `|`. |

```go
// Пользовательское ограничение, включающее int и float64
type Number interface {
	int | float64
}

// Функция, которая может суммировать только числовые типы
func SumNumbers[T Number](m map[string]T) T {
	// ... реализация
}
```

---

## 7. 💻 Инструменты (Tooling) #cli #tooling

Основные команды, используемые с исполняемым файлом `go`.

| Команда | Описание |
| :--- | :--- |
| `go run <файл.go>` | Компилирует и запускает указанный файл. |
| `go build` | Компилирует пакеты и создает исполняемый файл. |
| `go test` | Запускает тесты. |
| `go fmt` | Форматирует исходный код в стандартном стиле Go. |
| `go mod init <имя>` | Инициализирует новый модуль Go. |
| `go get <пакет>` | Добавляет необходимую зависимость. |
| `go install <пакет>` | Компилирует и устанавливает исполняемый файл. |

> [!TIP] Форматирование кода
> Всегда используйте `go fmt` для автоматического форматирования кода. Это обеспечивает единообразие стиля во всех Go-проектах.

---

## 8. 📚 Полезные Пакеты Стандартной Библиотеки #stdlib

| Пакет | Описание |
| :--- | :--- |
| `fmt` | Форматированный ввод/вывод. |
| `os` | Функции, зависящие от операционной системы (файлы, окружение). |
| `net/http` | Реализация HTTP-клиента и сервера. |
| `encoding/json` | Кодирование и декодирование JSON (`Marshal`, `Unmarshal`). |
| `time` | Функции для работы со временем и датами. |
| `sync` | Примитивы синхронизации (мьютексы, группы ожидания). |
| `context` | Управление контекстом для отмены операций в горутинах. |
| `testing` | Поддержка автоматизированного тестирования. |
| `strconv` | Преобразование базовых типов данных в строки и обратно. |

### Пример: Обработка Ошибок #ошибки

В Go ошибки — это обычные значения, которые реализуют встроенный интерфейс `error`.

```go
func Sqrt(x float64) (float64, error) {
	if x < 0 {
		// Создание новой ошибки
		return 0, fmt.Errorf("нельзя вычислить корень из отрицательного числа: %g", x)
	}
	return math.Sqrt(x), nil
}

func main() {
	result, err := Sqrt(-2)
	if err != nil {
		// Обработка ошибки
		fmt.Println("Ошибка:", err)
		return
	}
	fmt.Println("Результат:", result)
}
```

> [!NOTE] Идиома обработки ошибок
> Стандартная идиома Go: функция возвращает результат и ошибку. Если `err != nil`, то произошла ошибка, и результат игнорируется.

---

*Автор: Manus AI*
*Оптимизировано для Obsidian*
