---
tags: [golang, basics, packages, main]
aliases: [Go Hello World, Go Packages]
---

# 🌍 Hello World и Пакеты

## 1. Структура Программы Go

Каждая программа на Go состоит из **пакетов** (packages).

### Пакет `main`

Исполняемые программы должны использовать пакет `main`. Функция `main()` в этом пакете является точкой входа в программу.

```go
package main // Объявление пакета

import "fmt" // Импорт пакета для форматированного ввода/вывода

func main() { // Точка входа
	fmt.Println("Hello, World!")
}
```

### Импорт Пакетов

Для использования функций из других пакетов их необходимо импортировать.

| Синтаксис | Описание |
| :--- | :--- |
| `import "fmt"` | Импорт одного пакета. |
| `import ( "fmt"; "math" )` | **Факторизованный импорт** (рекомендуется). |
| `import f "fmt"` | Импорт с псевдонимом. |
| `import . "fmt"` | Импорт с точкой (не рекомендуется, загрязняет пространство имен). |

**Пример:**

```go
package main

import (
	"fmt"
	"math"
)

func main() {
	fmt.Println("Квадратный корень из 4:", math.Sqrt(4))
}
```

## 2. Экспорт Имен

В Go нет ключевых слов `public` или `private`. **Экспорт** имени (функции, переменной, типа) определяется регистром первой буквы:

*   **Заглавная буква:** Имя **экспортируется** (доступно из других пакетов).
*   **Строчная буква:** Имя **не экспортируется** (доступно только внутри своего пакета).

**Пример:**

```go
// В пакете "geometry"
func Area(w, h float64) float64 { // Экспортируется
	return w * h
}

func calculatePerimeter(w, h float64) float64 { // Не экспортируется
	return 2 * (w + h)
}
```

## 3. Компиляция и Запуск

*   **Запуск:** `go run main.go` (компилирует и запускает).
*   **Компиляция:** `go build main.go` (создает исполняемый файл).
*   **Инициализация модуля:** `go mod init <module_path>` (создает файл `go.mod` для управления зависимостями).
